# hermes-eslint-compatibility-test

Test `hermes-eslint` parser behavior across ESLint majors.

## Install

```sh
yarn install
```

## Fixture

Shared Flow parser fixture used by all packages:

`fixtures/sample.js`

## Run tests

Run the same supported versions:

```sh
yarn test:supported
```
